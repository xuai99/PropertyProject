<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="protect.myRentalInfo">

    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"/>

    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="com.google.android.providers.gsf.permission.READ_GSERVICES" />

    <permission
        android:name="com.pkg.map.permission.MAPS_RECEIVE"
        android:protectionLevel="signature" >
    </permission>
    <uses-feature
        android:glEsVersion="0x00020000"
        android:required="true"></uses-feature>
    <uses-feature
        android:name="android.hardware.camera"
        android:required="true" />

    <application

        android:allowBackup="false"
        android:icon="@drawable/ic_report"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:replace="android:allowBackup">
        <activity android:name=".cash_graph"
            android:configChanges="orientation|screenSize"
            android:label="Payment Breakdown"
            android:theme="@style/AppTheme.NoActionBar"
            ></activity>
        <meta-data
            android:name="com.google.android.gms.version"
            android:value="@integer/google_play_services_version" />
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/map_key" />

        <activity
            android:name=".google_map"
            android:configChanges="orientation|screenSize"
            android:label="@string/overviewTitle"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".graph"
            android:configChanges="orientation|screenSize"
            android:label="@string/overviewTitle"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".splash_screen"
            android:label="@string/app_name"
            android:screenOrientation="portrait"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".PropertiesListActivity"
            android:configChanges="orientation|screenSize"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".PropertyViewActivity"
            android:configChanges="orientation|screenSize"
            android:label="@string/addPropertyTitle"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".myPropertyOverviewActivity"
            android:configChanges="orientation|screenSize"
            android:label="@string/overviewTitle"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".PropertyWorksheetActivity"
            android:configChanges="orientation|screenSize"
            android:label="@string/worksheetTitle"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".PropertyNotesActivity"
            android:configChanges="orientation|screenSize"
            android:label="@string/notesTitle"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".myPropertyPicturesActivity"
            android:configChanges="orientation|screenSize"
            android:label="@string/picturesTitle"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity

            android:name=".myPropertyProjectionsActivity"
            android:configChanges="orientation|screenSize"
            android:label="@string/projectionsTitle"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".DictionaryActivity"
            android:configChanges="orientation|screenSize"
            android:label=""
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".myItemizeActivity"
            android:configChanges="orientation|screenSize"
            android:label=""
            android:theme="@style/AppTheme.NoActionBar"
            android:windowSoftInputMode="adjustPan" />
        <activity
            android:name=".PictureViewActivity"
            android:label=""
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".IntroActivity"
            android:label=""
            android:theme="@style/AppTheme.NoActionBar" />


    </application>

</manifest>